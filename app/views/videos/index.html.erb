<div class="row">
    <div class="col-12">
		<h4 class="image_list">画像一覧</h4>
		    <% @videos.each do |video| %>
		    <%= video_tag video.video_url.to_s,loop: true, controls: true, width: 550, id: "mv" %>
		    <% end %>
    </div>
